# js

## findModuleByName

通过名字来查找模块是否存在

```js
const m = Process.findModuleByName(target);
```

## hook方法，通过地址拦截调用和返回

```js
const addr = Module.findExportByName(null, name);
    if (!addr) return console.log("[-] no " + name);

    Interceptor.attach(addr, {
        onEnter(args) { this.path = args[0].readCString(); },
        onLeave(ret) {
            if (!this.path || this.path.indexOf(".so") === -1) return;
            // 只打印你关心的（你也可以删掉 if，打印全部）
            const p = this.path.toLowerCase();
            if (p.includes("il2cpp") || p.includes("unity") || p.includes("mono") || p.includes("main")) {
                console.log("[" + name + "] " + this.path + " ret=" + ret);
                setTimeout(probe, 50);
            }
        }
    });
    console.log("[+] hooked " + name);
}
//查看加载了哪些文件 
hookLoad("android_dlopen_ext");
hookLoad("dlopen");
```

## 打印线程调用栈

```js
console.log(Thread.backtrace(this.context, Backtracer.ACCURATE)
        .map(DebugSymbol.fromAddress).join('\n'));
```

## 方法参数


- 通过方法名字查找

```js
const UnitySendMessage = Module.findExportByName(
    "libunity.so",
    "UnitySendMessage"
);

Interceptor.attach(UnitySendMessage, {
    onEnter(args) {
        const obj = args[0].readCString();
        const method = args[1].readCString();
        const param = args[2].readCString();
        //加入
        if (
            method.includes("Click") ||
            method.includes("Press") ||
            method.includes("Down") ||
            method.includes("Up")
        ) {
            console.log("🖱 UnitySendMessage");
            console.log("  obj =", obj);
            console.log("  method =", method);
            console.log("  param =", param);
        }
    }
});
```

- 监听多个方法   通过地址

```js
'use strict';
//
Il2Cpp.perform(() => {
    // 🔥 你要监听的所有方法地址
    const targets = [
        ptr('0xb95a4320'),
        ptr('0xb95a4398'),
        ptr('0xb95a4e40'),
        ptr('0xb95a4ff8')
    ];
    console.log(`🚀 Hook ${targets.length} methods...`);
    targets.forEach(addr => {
        try {
            Interceptor.attach(addr, {
                onEnter(args) {
                    
                },
                // 如果你想看返回值，可以加 onLeave
                 onLeave(retval) {
                     console.log('↩ return =', retval);
                 }
            });

        } catch (e) {
            console.log('❌ Hook failed @', addr, e);
        }
    });
    console.log('✅ All hooks installed');
});
```

- 通过地址

```js
Interceptor.attach(
    ptr('0xb95ad024'),{
        onEnter(args) {
            console.log('🖱 UnitySendMessage');
            console.log('obj =', args[0].readCString());
            console.log('method =', args[1].readCString());
        },onLeave(retval) {
            console.log('💥 Force IsValid = true');
            retval.replace(1); // bool true
        }
    }
);
```

## 参数读取设置

读取参数，反正也没看懂

```js
'use strict';

const soName = "libcocos2dcpp.so"; // 按你的实际 so 改
const funcName = "_ZN5Level9cards2StrEb";

const addr = Module.findExportByName(soName, funcName);
console.log("[+] cards2Str @", addr);

Interceptor.attach(addr, {
    onEnter(args) {
        this.thisStr = args[0];   // std::string* (返回值)
        this.level   = args[1];   // Level*
        this.flag    = args[2].toInt32();

        const offset = this.flag ? 88 : 76;
        const vec = this.level.add(offset);

        this.begin = vec.readPointer();
        this.end   = vec.add(4).readPointer();

        const count = this.end.sub(this.begin).toInt32() / 24;

        console.log("🟦 cards2Str");
        console.log(" flag =", this.flag);
        console.log(" cardCount =", count);

        // 打印每张卡的原始数据
        for (let i = 0; i < count; i++) {
            const card = this.begin.add(i * 24);
            console.log(`  Card[${i}] @ ${card}`);
            dumpCard(card)
        }
    },

    onLeave(retval) {
        try {
            // std::string 内容
            const strPtr = this.thisStr;
            const isSmall = strPtr.readU8() & 1;

            let dataPtr, len;
            if (isSmall) {
                len = strPtr.readU8() >> 1;
                dataPtr = strPtr.add(1);
            } else {
                dataPtr = strPtr.add(8).readPointer();
                len = strPtr.add(16).readU32();
            }

            const result = dataPtr.readUtf8String(len);
            console.log(" 🟢 RESULT =", result);
        } catch (e) {
            console.error("read string failed", e);
        }
    }
});

function dumpCard(ptr) {
    try {
        const begin = ptr.add(8).readPointer();  // begin 指针
        const end   = ptr.add(12).readPointer(); // end 指针
        const count = end.sub(begin).toInt32() / 4; // 每张卡 4 字节

        const cards = [];
        for (let i = 0; i < count; i++) {
            const val = begin.add(i * 4).readS32();
            cards.push(val);
        }

        console.log("🧩 Card Values =", cards);
    } catch (e) {
        console.error("dumpCard failed", e);
    }
}
```