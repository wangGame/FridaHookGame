function() {        const container = document.querySelector('.build-info-container.page-container');        if (container) {            const keys = container.querySelectorAll('.build-information-key');            for (let key of keys) {                if (key.textContent.trim() === 'campaign:') {                    const value = key.nextElementSibling;                    return value ? value.textContent.trim() : null;                }            }        }        return null;    })();