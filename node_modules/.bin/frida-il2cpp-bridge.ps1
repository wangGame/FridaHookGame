#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/python3$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/python3$exe"  "$basedir/../frida-il2cpp-bridge/cli/main.py" $args
  } else {
    & "$basedir/python3$exe"  "$basedir/../frida-il2cpp-bridge/cli/main.py" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "python3$exe"  "$basedir/../frida-il2cpp-bridge/cli/main.py" $args
  } else {
    & "python3$exe"  "$basedir/../frida-il2cpp-bridge/cli/main.py" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
